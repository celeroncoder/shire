import AppKit

extension NSColor {
    /// Sidebar background with vibrancy
    static let sidebarBackground = NSColor.controlBackgroundColor

    /// Chat bubble colors
    static let userBubble = NSColor.systemBlue
    static let assistantBubble = NSColor.controlBackgroundColor

    /// Accent colors
    static let accentPrimary = NSColor.controlAccentColor
    static let subtleText = NSColor.tertiaryLabelColor
    static let borderColor = NSColor.separatorColor
}
